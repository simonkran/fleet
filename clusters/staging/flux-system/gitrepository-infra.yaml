---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: infra
  namespace: flux-system
  labels:
    environment: ${ENVIRONMENT}
    cluster: ${CLUSTER_NAME}
spec:
  interval: ${RECONCILE_INTERVAL}
  url: https://github.com/${GITHUB_ORG}/fleet-infra.git
  ref:
    branch: ${INFRA_REF}
  secretRef:
    name: flux-system
  ignore: |
    # exclude all
    /*
    # include clusters dir
    !/clusters
